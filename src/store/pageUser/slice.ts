import { createSlice } from "@reduxjs/toolkit";

const initialState ={

}

export const {reducer:pageUserReducer,actions:pageUserActions}=createSlice({
    name:"PageUser",
    initialState,
    reducers:{
        
    }
})